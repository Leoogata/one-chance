base_y = y;   
base_x = x;  // posição inicial (centro do movimento)
amplitude = 5;         // quanto vai oscilar (10 pixels para cima/baixo)
velocidade_osc = 0.05;  // velocidade da oscilação
tempo = random(360);    // fase inicial (para não ficarem iguais)